# paleontology-notebook

A notebook illustrating how to use bash to dig around in repos.

To extract all bash commands from the notebook into a shell script,
run this:

```jupyter nbconvert --no-prompt --to script stats.ipynb```
